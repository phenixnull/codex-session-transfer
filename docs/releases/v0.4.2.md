# Codex Session Transfer v0.4.2

## Fixes

- Session migration now resolves a provider display name to the provider ID configured in `config.toml`.
- Imported SQLite rows and rollout metadata consistently use the resolved target provider ID.
- Overwrite matching works across provider switches and replaces the complete matching conversation data.
- Invalid configured provider IDs now produce an explicit configuration error.

## Verification

- 51 Python unit and transaction tests
- Frontend mapping, overwrite-control, IPC, responsive-layout, window-constraint, and release-workflow checks
- Windows package build completed with SHA-256 manifest generation
